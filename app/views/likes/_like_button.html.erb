<% if current_user.like_post?(@post) %>
  <%= form_for(current_user.likes.find_by(post_id: @post.id), html: { method: :delete }) do |f| %>
    <%= hidden_field_tag :post_id, @post.id %>
    <%= f.submit 'Unike', class: "btn btn-outline-danger btn-sm" %>
  <% end %>
<% else %>
  <%= form_for(current_user.likes.build) do |f| %>
    <%= hidden_field_tag :post_id, @post.id %>
    <%= f.submit 'Like!', class: "btn btn-outline-danger btn-sm" %>
  <% end %>
<% end %>